// Extensions for the cimgui API.
// Nothing fancy, I don't really care.

#pragma once

#include "imgui.h"
/*#include "imgui_markdown/imgui_markdown.h"

FANTY_API void igMarkdown(const char* markdown)
{
	ImGui::MarkdownConfig mdConfig;
	std::string markdownText = markdown;

	ImGui::Markdown(markdownText.c_str(), markdownText.length(), mdConfig);
}
*/